export default function GamePage() {
  //check if this is a valid room, if not, redirect to the landing with a toast
  // listen for this room events, change in room size, deletion, status change name changes
  // after the game starts, listen for player changes. render the result after the match is done
  return <section></section>;
}
